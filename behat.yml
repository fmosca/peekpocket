default:
    suites:
        cli_features:
            paths:    [ %paths.base%/features/cli ]
            contexts: 
                - CliContext:
                    root: %paths.base% 
        api_features:
            paths:    [ %paths.base%/features/api ]
            contexts: 
                - ApiContext:
                    root: %paths.base%
